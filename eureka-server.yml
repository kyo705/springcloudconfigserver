server:
  port: 8761 ##포트번호는 실행 중일때 변경 시 적용되지 않음 -> actuator 적용안됌

spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  endpoints:
    web:
      exposure:
        include: refresh
