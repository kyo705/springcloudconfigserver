profile: lee sunkyov2

server:
  port: 9001

spring:
  application:
    name: summonerService
        
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
management:
  endpoints:
    web:
      exposure:
        include: refresh
